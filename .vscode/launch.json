{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Current file",
            "runtimeArgs": [
                "-r",
                "ts-node/register"
            ],
            "cwd": "${workspaceFolder}/src",
            "args": ["${relativeFile}"],
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal",
            "program": "${workspaceFolder}/src/chroma/RelevantInfo.ts"
        }
    ]
}
